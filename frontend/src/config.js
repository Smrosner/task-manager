const config = {
  development: {
    baseURL: "http://127.0.0.1:5000/",
  },
  production: {
    // Todo: update with production base url
    baseURL: "https://your-production-url.com/",
  },
};

export default config;
